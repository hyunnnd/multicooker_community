# 2026-07-23 디자인 및 기능 복구 기준

기준 UI는 `GrapheneSquare_cooker_app_community_header_refined(3).zip`입니다.

복구한 항목:
- 레시피 목록/상세의 실제 DB 별점, 후기 수, 사용 횟수 표시
- 레시피 관리의 `내 레시피 / 저장한 레시피` 탭 UI
- 작성자 프로필 화면 및 작성자 프로필 이동 경로
- 커뮤니티 목록, 게시글 상세, 공지, 댓글/답글, 가로형 더보기 메뉴 디자인
- 마이페이지 및 설정 하위 화면 디자인
- 홈 진입 시 조리 이력 개수 즉시 조회
- 닉네임/프로필 이미지는 로컬 프로필 값을 최종 표시값으로 사용
- owner_user_id PK 우선 소유권 판정 및 임시 업로드/orphan 정리 백엔드

펫 처리:
- `tangerine_chef.webp`, `tangerine_idle.png` 두 파일만 코드에서 사용
- 두 파일 모두 8열 x 9행, 프레임 192 x 208 구조로 상태별 애니메이션 재생
- 인접 프레임 픽셀이 비치는 현상을 막기 위해 반 픽셀 안쪽을 샘플링
- 원본 펫 이미지 파일은 수정하지 않음

기기 관리:
- 쿠커 연결 성공 시 연결 완료 팝업 없이 `/home`으로 즉시 이동

배포 ZIP에는 `multicooker.db`, WAL/SHM, `local_uploads`를 포함하지 않습니다.
